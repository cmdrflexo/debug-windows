/*
 * Defines the shared visibility modes used by optional component gizmos.
 */

namespace jcan.CelestialSystems
{
    public enum GizmoDrawMode
    {
        Hidden = 0,
        Always = 1,
        Selected = 2
    }
}
